machine:
  python:
    version: "3.5.2"

dependencies:
  pre:
    - pip install pytest pytest-cov
  override:
    - pip install .

test:
  pre:
    - find . -name '__pycache__' | xargs rm -rf
  override:
    - py.test -v --cov=jacquard jacquard
